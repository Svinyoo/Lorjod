# AGENTS.md — รอจอด / Parkly

คู่มือสำหรับผู้พัฒนาและ AI Agent ใน repository นี้ ครอบคลุมทุกโฟลเดอร์ เว้นแต่มี `AGENTS.md` ในโฟลเดอร์ย่อยกำหนดรายละเอียดเฉพาะไว้

เอกสารนี้อ้างอิงโค้ดที่ตรวจสอบเมื่อ 20 กันยายน 2026 คำว่า **ปัจจุบัน** หมายถึงสิ่งที่มีในโค้ด ส่วน **แนวทาง** และ **ก่อน production** เป็นข้อกำหนดสำหรับงานต่อยอด ไม่ใช่การยืนยันว่าระบบทำแล้ว หากโค้ดกับเอกสารขัดกัน ให้รายงานความต่างก่อนเปลี่ยนพฤติกรรมทางธุรกิจ

## 1. Project Overview

- Repository หลักคือ `https://github.com/Svinyoo/Lorjod` ใช้ชื่อ Parkly ใน UI และ README และชื่อ “รอจอด” ในบริบทโครงการ อย่าเปลี่ยนชื่อแบรนด์เอง
- พาธทั้งหมดในเอกสารนี้อ้างอิงจากราก repository ที่มีไฟล์นี้ ไม่ขึ้นกับชื่อโฟลเดอร์ที่ใช้ clone
- เป็นเว็บแอปสำหรับผู้เช่า ผู้ให้เช่า และผู้ดูแลระบบ ใช้ข้อมูลลาน ช่องจอด รถ และการจองร่วมกัน
- หาก checkout อยู่ภายใน workspace ที่มี Git repository อีกชุด ให้ตรวจ `git rev-parse --show-toplevel` และ `git remote -v` ก่อนทำงาน โดยเป้าหมายต้องเป็น repo `Svinyoo/Lorjod` ไม่ใช่ repository ของโฟลเดอร์แม่
- ระบบจองยังเป็นต้นแบบ: การชำระเงิน, QR สำหรับประตูจริง และการส่ง push ยังไม่ครบสำหรับ production

## 2. Business Goals

เป้าหมายเชิงผลิตภัณฑ์คือช่วยผู้ขับขี่ค้นหาและจองช่องจอดโดยเห็นราคาและช่วงเวลาชัดเจน ช่วยเจ้าของลานจัดการช่องจอดและการเข้า–ออก และให้ผู้ดูแลแก้ไขข้อมูลพร้อมตรวจสอบย้อนหลังได้

- ให้ข้อมูลราคาและช่องว่างสอดคล้องกันระหว่างทุกบทบาท
- ป้องกันการจองซ้อนและการเข้าถึงข้อมูลข้ามเจ้าของ
- รักษาประวัติการจองและเหตุผลการแก้ไขข้อมูล
- ยังไม่มีเป้าหมายรายได้ ค่าคอมมิชชัน หรือ KPI ตัวเลขที่ยืนยันแล้ว ห้ามสร้างกติกาเหล่านี้เอง

## 3. Target Users

- **Renter:** ผู้ขับรถยนต์ รถ EV หรือจักรยานยนต์ที่ต้องการจองที่จอด
- **Landlord:** เจ้าของหรือผู้ดำเนินการลานที่จัดการลานและช่องจอดของตนเอง
- **Admin:** ผู้ดูแลที่ตรวจสอบและแก้ไขข้อมูลผ่านหน้าผู้ดูแล พร้อมบันทึกประวัติ
- ปัจจุบันไม่มีบทบาทพนักงานประจำประตู องค์กร หรือผู้ให้เช่าหลายคนร่วมดูแลลานเดียวกัน

## 4. Core Features

- สมัครสมาชิก เข้าสู่ระบบ ออกจากระบบ และตรวจ session
- ค้นหาลานที่เปิดรับจอง ดูราคา พิกัด จุดสังเกต และจำนวนช่องว่างโดยประมาณ
- เพิ่มรถ เลือกรถคันโปรด ขอราคา จอง ดู Digital Pass และต่อเวลาจอด
- ผู้ให้เช่าสร้างลานฉบับร่าง เพิ่มช่องจอด เปิด/ปิดรับจอง และบันทึกเข้า–ออก
- Admin แก้ข้อมูลผู้ใช้ ลาน ช่องจอด รถ และการจอง พร้อมเหตุผลและ audit
- สร้างรายการแจ้งเตือนในฐานข้อมูล ยังไม่มี worker ส่งจริง
- การโหลดข้อมูลปัจจุบันเป็น HTTP request ไม่ใช่ WebSocket และไม่ใช่ข้อมูลจากเซนเซอร์หน้างาน

## 5. Parking Business Rules

กติกาปัจจุบันที่ต้องรักษา เว้นแต่งานระบุให้เปลี่ยน:

- ประเภทรถใช้ `car`, `ev`, `motorcycle` และต้องตรงกับประเภทช่องจอด รถต้องเป็นของผู้เช่าที่จอง
- การขอราคาและสร้างการจองต้องมีเวลาเริ่มก่อนเวลาสิ้นสุด ระยะเวลาไม่เกิน 24 ชั่วโมง
- API รับเวลาแล้วปรับเป็น ISO 8601 UTC; ช่วงเวลาจองใช้ขอบเขต `[start, end)` จึงจองต่อกันตรงเวลาสิ้นสุดได้
- การซ้อนทับคือ `existing.start_at < requested.end_at AND existing.end_at > requested.start_at` โดยนับสถานะ `confirmed` และ `active`
- ราคาจองใหม่คือ `Math.ceil(durationMilliseconds / 3600000) * hourly_rate` หน่วยเงินปัจจุบันคือบาทจำนวนเต็ม ห้ามเปลี่ยนเป็นสตางค์โดยไม่มี migration และการปรับสัญญา API
- ฝั่ง server คำนวณราคาและเลือกช่องว่างใหม่ตอนจองเสมอ ใบเสนอราคาไม่ใช่การกันช่อง
- รายการค้นหาแสดงช่องว่างช่วงปัจจุบันถึงอีก 2 ชั่วโมง ส่วน quote ตรวจตามประเภทและช่วงเวลาที่เลือก ห้ามอธิบายสองค่านี้ว่าเหมือนกันทุกกรณี
- ลานใหม่ของ Landlord เป็นฉบับร่าง ต้องมีช่องก่อนเปิดรับจอง การปิดลานไม่ลบการจองเดิม
- เปลี่ยนราคามีผลกับการจองใหม่ การต่อเวลาคิดเพิ่มตามราคาปัจจุบันของลาน
- ต่อเวลาได้ครั้งละจำนวนเต็ม 1–8 ชั่วโมง เฉพาะ `confirmed`/`active` และต้องไม่ชนรายการถัดไป ปัจจุบันเส้นทางนี้ไม่ได้จำกัดเวลารวมไว้ที่ 24 ชั่วโมง
- Landlord เปลี่ยนสถานะได้เฉพาะ `confirmed → active → completed` การเข้าจอดต้องอยู่ในช่วงเวลาจอง
- Admin แก้สถานะรวมถึง `cancelled` ได้ภายใต้ validation, การตรวจข้อมูลเก่า และ audit ไม่ใช่สิทธิ์ยกเลิกด้วยตนเองของ Renter
- ลานตัวอย่างเดิมไม่มีเจ้าของ ห้ามโอนให้ผู้สมัครใหม่อัตโนมัติ
- ยังไม่มีกติกาที่ตกลงสำหรับ no-show, ค่าปรับเกินเวลา, grace period, ภาษี, ส่วนลด, คืนเงิน หรือการห้ามจองย้อนหลัง อย่าเพิ่มเอง
- ข้อจำกัดปัจจุบัน: การตรวจช่องและเขียน booking ของ Renter ยังไม่เป็น transaction ที่รับประกันป้องกัน concurrent booking ต้องแก้ก่อนอ้างว่ารองรับการจองพร้อมกันได้อย่างปลอดภัย

## 6. User Roles & Permissions

| บทบาท | ขอบเขตสิทธิ์ |
| --- | --- |
| ยังไม่เข้าสู่ระบบ | สมัคร Renter/Landlord และเข้าสู่ระบบ; เรียก auth/me เพื่อตรวจ session |
| Renter | ค้นหาลาน ขอราคา จัดการรถและอ่าน/สร้าง/ต่อเวลาการจองของตนเอง |
| Landlord | จัดการลาน ช่องจอด และการจองเฉพาะลานที่ตนเป็นเจ้าของ |
| Admin | อ่านข้อมูลผ่าน Admin API และแก้เฉพาะ entity/field ที่อนุญาต พร้อมเหตุผลและ audit |

- ตรวจทั้งบทบาทและเจ้าของข้อมูลที่ backend ทุกครั้ง การซ่อนปุ่มไม่ใช่การบังคับสิทธิ์
- Admin ไม่มีสิทธิ์เปลี่ยน role/password ผ่านหน้าจัดการผู้ใช้ และไม่ได้รับสิทธิ์เรียก Renter/Landlord API โดยอัตโนมัติ
- ไม่มีหน้าสมัคร Admin; สร้างผ่าน `server/create-admin.js` ด้วยผู้ดำเนินการที่ได้รับอนุญาตเท่านั้น

## 7. User Flow

1. **Renter:** สมัคร/เข้าสู่ระบบ → ค้นหาลาน → เลือกเวลาและรถ → ขอราคา → ตรวจสรุปและเลือกช่องทางชำระเงินแบบเดโม → ยืนยัน → รับ Pass → ดูเวลาคงเหลือ/ต่อเวลา
2. **Landlord:** สมัคร/เข้าสู่ระบบ → สร้างลานฉบับร่าง → เพิ่มช่อง → เปิดรับจอง → ดูรายการ → ตรวจ Pass → บันทึกเข้า → บันทึกออก
3. **Admin:** เข้าสู่ระบบ → โหลดข้อมูล → เปิดแก้ไข → ระบุเหตุผล → บันทึก → ตรวจผลและ audit หากข้อมูลเปลี่ยนระหว่างแก้ไข ต้องโหลดข้อมูลใหม่

แต่ละบทบาทใช้ session cookie เดียวกันใน browser profile เดียว หากทดสอบพร้อมกันให้แยก profile หรือ cookie jar

## 8. System Architecture

```text
Browser: Renter / Landlord / Admin (HTML + CSS + JavaScript)
                         │ same-origin HTTP / JSON + session cookie
                  Express: server/index.js
                         │
             auth / renter / landlord / admin routers
                         │
                  SQLite: DATA_DIR/app.db
```

- Express เสิร์ฟทั้ง static pages และ API; router แต่ละบทบาทรับ `db` แล้วคืน Express Router
- Admin ใช้ connection แยกไปไฟล์ฐานข้อมูลเดียวกัน และ queue การแก้ไขด้วย `BEGIN IMMEDIATE` เพื่อแยก transaction
- ปัจจุบัน session ใช้ค่าเริ่มต้นของ `express-session` ซึ่งเก็บในหน่วยความจำ ยังไม่มี shared session store
- ยังไม่มี payment service, notification worker, message queue หรือบริการตรวจประตูจริง

## 9. Tech Stack

| ส่วน | เทคโนโลยีปัจจุบัน |
| --- | --- |
| Runtime | Node.js >=20.17.0; tested with Node.js 24 (`.nvmrc`) |
| Backend | Express 5, JavaScript แบบ CommonJS |
| Database | SQLite ผ่าน `sqlite` และ `sqlite3` |
| Authentication | `express-session`, `bcryptjs` |
| Frontend | HTML, CSS, Vanilla JavaScript ไม่มีขั้นตอน build |
| Tests | `node:test`, `node:assert/strict`, HTTP และฐานข้อมูลชั่วคราว |
| Dependencies | npm และ package-lock.json |

อย่าเพิ่ม framework, ORM, TypeScript หรือเปลี่ยนฐานข้อมูลทั้งระบบเพียงเพื่อแก้งานเฉพาะจุด

## 10. Project Structure

```text
Lorjod/                # ชื่อโฟลเดอร์ clone เปลี่ยนได้
  AGENTS.md
  README.md
  package.json / package-lock.json
  server/
    index.js           # startup, static routes, session, error middleware
    database.js        # connection, schema, additive migrations, seed
    auth.js            # register/login/logout/me
    renter.js          # vehicles, locations, quotes, bookings, extension
    landlord.js        # owner-scoped locations, spots, booking status
    admin.js           # controlled edits, conflict checks, audit
    create-admin.js    # server-side administrative command
  client_Renter/       # ชื่อไดเรกทอรีที่พบจริงใน working tree
  Client_Landlord/
  Client_Admin/
    public/
    README.md
  tests/               # renter, landlord, admin, database
  data/app.db          # สร้างขณะรัน หรือเปลี่ยนที่เก็บด้วย DATA_DIR
```

Use `client_Renter` consistently in directory names, static routes, and documentation for case-sensitive filesystems.

## 11. Database & Data Model

| ตาราง | ข้อมูลและความสัมพันธ์ |
| --- | --- |
| `users` | ชื่อ อีเมลไม่ซ้ำ password_hash และ role |
| `parking_locations` | ลาน พิกัด landmarks แบบ JSON string ราคา owner_id และ is_published |
| `parking_spots` | อ้างลาน มีประเภทและ spot_label ที่ไม่ซ้ำภายในลาน |
| `vehicles` | อ้างผู้ใช้ ทะเบียน ประเภทรถ รายละเอียด และรถคันโปรด |
| `bookings` | อ้างผู้ใช้ ลาน ช่อง รถ; เวลา ยอด สถานะ ช่องทางชำระ และ pass_code ไม่ซ้ำ |
| `notifications` | อ้างผู้ใช้/การจอง มี kind, scheduled_at และ sent_at |
| `admin_audit` | ผู้แก้ entity/record เหตุผล ค่า before/after และเวลา |

- เปิด foreign keys ในทุก connection; รักษา index `bookings_slot_range` ซึ่งช่วย query แต่ไม่ได้รับประกันว่าการจองไม่ซ้อน
- ใช้ parameterized SQL สำหรับข้อมูลจากผู้ใช้ ชื่อตาราง/คอลัมน์แบบ dynamic ต้องมาจาก allowlist
- เพิ่ม migration ใน `database.js` แบบรันซ้ำได้และรักษาข้อมูลเดิม ห้าม drop/recreate เพื่อแก้ schema บนข้อมูลใช้งาน
- การแก้หลายตารางที่ต้องสำเร็จร่วมกันควรเป็น transaction พร้อม rollback และจัดการ connection ไม่ให้คำขออื่นแทรก
- เวลาจอง/แจ้งเตือนใช้ ISO UTC; timestamp ที่ได้จาก `CURRENT_TIMESTAMP` เป็นข้อความ UTC ของ SQLite ให้แปลงอย่างชัดเจนก่อนแสดงผล
- การเปลี่ยน schema ต้องทดสอบทั้งฐานข้อมูลใหม่และ migration จาก schema เดิม

## 12. API Guidelines

- คง prefix: `/api/auth`, `/api` สำหรับ Renter, `/api/landlord`, `/api/admin`
- ใช้ GET อ่าน, POST สร้าง/ดำเนินการ และ PATCH แก้ไข ตามรูปแบบ endpoint เดิม
- รักษา request/response ของ endpoint ที่มีอยู่: Renter ใช้ camelCase หลายส่วน ขณะที่ Admin และ Landlord บางส่วนคืน snake_case อย่าปรับทั้งระบบโดยไม่ตรวจผู้เรียก
- ตรวจ type, enum, ID, ความยาว, ค่าตัวเลขที่เป็น finite และช่วงเวลาก่อนใช้ข้อมูล
- ใช้ `201` สำหรับสร้าง, `204` สำหรับสำเร็จไม่มี body, `400` ข้อมูลไม่ถูกต้อง, `401` ไม่มี session, `403` ผิดสิทธิ์, `404` ไม่พบในขอบเขตผู้ใช้, `409` conflict และ `500` ข้อผิดพลาดภายใน
- error body ใช้ `{ "message": "ข้อความที่ผู้ใช้เข้าใจได้" }`; บาง business conflict ของ Renter ปัจจุบันใช้ 400 จึงต้องตรวจ compatibility ก่อนเปลี่ยน
- สำหรับรายการที่โตขึ้น ให้เพิ่ม pagination/filter อย่างมีขอบเขตและปรับ client พร้อมกัน

## 13. Authentication & Authorization

- ใช้ session ฝั่ง server ต่อไป เว้นแต่มีข้อกำหนดเปลี่ยนระบบ authentication
- ปัจจุบันรหัสผ่านทั่วไปขั้นต่ำ 8 ตัวอักษร; คำสั่งสร้าง Admin ขั้นต่ำ 12 ตัวอักษร ใช้ bcrypt hash และไม่ส่ง hash กลับ client
- รักษา session regeneration หลังเข้าสู่ระบบ/สมัคร และทำลาย session เมื่อออกจากระบบ
- cookie ปัจจุบันใช้ `httpOnly`, `sameSite: lax`, อายุ 24 ชั่วโมง และ `secure` เมื่อ `NODE_ENV=production`
- อ่าน identity จาก session ไม่รับ `user_id`, `owner_id` หรือ role จาก client เพื่อข้ามสิทธิ์; การกำหนด owner ผ่าน Admin ต้องผ่าน validation ของ Admin
- ก่อน production ต้องกำหนด `SESSION_SECRET` จริง ใช้ session store ที่เหมาะสม และตรวจ HTTPS/proxy configuration

## 14. Frontend Guidelines

- แก้หน้าและ JavaScript ภายในโฟลเดอร์ของบทบาทนั้น รักษา URL `/`, `/landlord/`, `/admin/`
- ใช้รูปแบบ fetch/auth ที่มีอยู่ จัดการ loading, empty, success และ error ให้ครบ
- แสดงข้อมูลจากผู้ใช้ด้วย `textContent` หรือ escape ตามบริบท หลีกเลี่ยงนำค่าที่ไม่เชื่อถือเข้า `innerHTML`
- validation ฝั่ง client มีไว้ช่วยผู้ใช้ ส่วนราคา สิทธิ์ และสถานะยึดผลจาก backend
- กันการกดส่งซ้ำระหว่างคำขอและคืนสถานะปุ่มเมื่อผิดพลาด; การกันซ้ำใน UI ไม่ทดแทน idempotency ฝั่ง server
- อย่าเก็บ password, session token หรือข้อมูลชำระเงินใน localStorage

## 15. Backend Guidelines

- เพิ่ม endpoint ใน router ตามบทบาท รักษารูปแบบ dependency injection ผ่าน `db`
- แยก validation และกติกาที่ใช้ซ้ำเมื่อมีเหตุผลจริง ไม่สร้าง abstraction ขนาดใหญ่โดยไม่จำเป็น
- ตรวจเจ้าของใน query และตรวจความสัมพันธ์ผู้ใช้–รถ–ช่อง–ลานก่อนบันทึก
- รักษา Admin transaction, stale-data check และ audit ให้สำเร็จหรือ rollback ร่วมกัน
- เมื่อแก้ concurrency ต้องทดสอบคำขอพร้อมกันจริง การตรวจ SELECT ก่อน INSERT อย่างเดียวไม่เพียงพอ
- ไม่ทำ network call ที่รอนานอยู่ภายใน database transaction

## 16. UI/UX Guidelines

- ใช้ภาษาไทยที่กระชับสำหรับผู้ใช้ แสดงบทบาท สถานที่ ทะเบียน วันเวลา ระยะเวลา และยอดเงินให้ชัดเจน
- รักษารูปแบบหน้าปัจจุบันของแต่ละบทบาท และออกแบบให้ใช้ได้ทั้งมือถือและเดสก์ท็อป
- แสดงเงินบาทอย่างสม่ำเสมอ; แนวทางสำหรับเวลาแสดงผลคือ `Asia/Bangkok` พร้อม label ที่ชัดเจน โดยไม่เปลี่ยนเวลา UTC ที่จัดเก็บ
- มี label สำหรับ input, focus ที่มองเห็นได้, การใช้คีย์บอร์ด และข้อความอธิบายสถานะที่ไม่พึ่งสีเพียงอย่างเดียว
- ก่อนยืนยันการจองหรือต่อเวลา ให้ผู้ใช้เห็นยอดและช่วงเวลาที่จะบันทึก
- สถานะเต็ม ข้อมูลเปลี่ยน หรือ session หมดอายุต้องมีทางดำเนินการต่อ เช่น เลือกเวลาใหม่ โหลดข้อมูลใหม่ หรือเข้าสู่ระบบ
- อย่าใช้ข้อความว่า “ชำระแล้ว” หรือ “ส่งแจ้งเตือนแล้ว” หากยังไม่มีหลักฐานจากระบบจริง

## 17. Coding Standards

- ใช้ JavaScript, `const` เป็นหลัก และ `async/await` ให้สอดคล้องโค้ดบริเวณที่แก้
- ทำตาม indentation/quote style ของไฟล์ หลีกเลี่ยง format ทั้งไฟล์เมื่อแก้เฉพาะจุด
- ตั้งชื่อให้สื่อความหมาย ใช้คอมเมนต์อธิบายเหตุผลหรือข้อจำกัด ไม่เขียนซ้ำสิ่งที่โค้ดบอกอยู่แล้ว
- ไม่ hard-code credentials, absolute path ของเครื่องผู้พัฒนา หรือ URL production
- ไม่เพิ่ม dependency โดยไม่มีความจำเป็น หากเพิ่มต้องอัปเดต manifest และ lockfile ให้ตรงกัน
- ปัจจุบันไม่มี lint/build script อย่าอ้างว่าได้รันคำสั่งที่ไม่มีอยู่

## 18. Testing Guidelines

รันคำสั่งจากราก repository `Svinyoo/Lorjod` ที่มี `package.json` และไฟล์นี้:

```sh
npm test
node --test tests/renter.test.js
node --test tests/landlord.test.js
node --test tests/admin.test.js
node --test tests/database.test.js
```

- เลือกรันชุดที่เกี่ยวข้องระหว่างพัฒนา และรันทุกชุดเมื่อเปลี่ยนกติกาจอง สิทธิ์ schema หรือโค้ดที่หลายบทบาทใช้ร่วมกัน
- ใช้ `DATA_DIR` ชั่วคราวและพอร์ตแยก ห้ามใช้หรือล้าง `data/app.db` จริงเพื่อทดสอบ
- เพิ่ม regression test สำหรับ bug และพฤติกรรมสำคัญที่เปลี่ยน ไม่ต้องเพิ่ม test สำหรับการแก้เอกสารอย่างเดียว
- กรณีสำคัญ: ข้ามเจ้าของ, ผิดบทบาท, เวลาไม่ถูกต้อง/ตรงขอบเขต, ช่องเต็ม, จองซ้อน, ต่อเวลาชน, ราคาปัดขึ้น, migration และ audit rollback
- งาน UI ต้องตรวจหน้าจอและ flow ที่เกี่ยวข้องเพิ่มเติม เพราะ HTTP tests ไม่ใช่ browser tests
- รายงานคำสั่งและผลจริง หากรันไม่ได้ให้ระบุสาเหตุ ห้ามสรุปว่าผ่านจากการอ่านโค้ด

## 19. Error Handling

- แยก validation/business errors ออกจาก unexpected errors ไม่ส่ง stack trace, SQL หรือรายละเอียดฐานข้อมูลให้ผู้ใช้
- ใช้ error middleware กลางสำหรับข้อผิดพลาดภายใน พร้อมข้อความภาษาไทยที่นำไปดำเนินการต่อได้
- โค้ด Renter บางส่วนจับ error ทั้งหมดแล้วคืน `e.message` เป็น 400; เมื่อทำงานส่วนนี้ควรแยก error ที่คาดไว้ให้ชัดเจน ไม่ขยายรูปแบบดังกล่าวไป endpoint ใหม่
- บันทึก context ที่จำเป็นสำหรับ debug โดยตัดข้อมูลอ่อนไหว และ rollback ก่อนตอบเมื่อ transaction ไม่สำเร็จ
- ไม่ retry การสร้าง booking หรือการเงินแบบไม่ตรวจผลเดิม เพราะอาจทำรายการซ้ำ

## 20. Security & Privacy

- ปฏิบัติต่อชื่อ อีเมล ทะเบียนรถ พิกัด และประวัติการจองเป็นข้อมูลส่วนบุคคล จำกัดการอ่านตามหน้าที่
- ไม่ commit `.env`, credentials, session data, ฐานข้อมูลจริง หรือ log ที่มีข้อมูลส่วนบุคคล ตรวจ ignore rules ก่อนเพิ่มไฟล์
- ไม่เสิร์ฟโฟลเดอร์ server/data เป็น static และไม่ใส่ password hash ลง response หรือ audit
- งานก่อน production: ตรวจ CSRF สำหรับ cookie authentication, rate limit การ login/สมัคร/ตรวจ Pass, secure headers และการจัดเก็บ session
- Pass ปัจจุบันและ QR-style visual เป็นเดโม ก่อนใช้เปิดประตูจริงต้องมี token ที่ตรวจสอบได้ อายุจำกัด และป้องกันการใช้ซ้ำตามกติกาที่ตกลง
- อายุเก็บข้อมูล การลบข้อมูล การเข้าถึง audit และนโยบายสำรองข้อมูลยังต้องกำหนด อย่าอ้างว่าระบบรองรับข้อกำหนดด้านความเป็นส่วนตัวครบแล้ว

## 21. Payment Guidelines

- ปัจจุบันบันทึก `payment_method` และสร้าง booking เป็น `confirmed` โดยไม่มี payment gateway หรือหลักฐานรับเงินจริง
- ยอดรวมฝั่ง Landlord เป็นมูลค่าการจอง ไม่ใช่ยอดรับชำระหรือยอดโอนให้เจ้าของลาน
- Admin แก้ `total` เป็นการแก้ข้อมูล ไม่ใช่การเรียกเก็บหรือคืนเงิน
- หากต่อระบบจริง ต้องตกลง provider, หน่วยเงิน, ค่าธรรมเนียม, ภาษี, วิธีคืนเงิน และ booking/payment state ก่อน
- คำนวณยอดที่ backend ใช้จำนวนเต็มตามหน่วยเงินที่กำหนด และยืนยันผลผ่าน webhook ที่ตรวจลายเซ็น ยอด สกุลเงิน และ reference
- webhook/การสร้างรายการต้องรองรับ idempotency และเหตุการณ์ซ้ำหรือมาผิดลำดับ ห้ามยืนยันชำระจาก redirect หรือคำตอบ client เพียงอย่างเดียว
- ไม่เก็บข้อมูลบัตรหรือ secret ของ gateway ใน frontend ใช้ sandbox และข้อมูลทดสอบสำหรับการพัฒนา

## 22. Notification Guidelines

- เมื่อจอง ระบบสร้าง `arrival_reminder` ก่อนเริ่ม 30 นาที และ `expiry_reminder` ก่อนสิ้นสุด 15 นาที
- `notifications` เป็นกำหนดการในฐานข้อมูล ไม่ใช่หลักฐานว่าส่งถึงผู้ใช้แล้ว
- Admin ปรับกำหนดการที่ยังไม่ส่งเมื่อแก้ booking; การต่อเวลาฝั่ง Renter ปัจจุบันยังไม่ได้เลื่อน expiry reminder ให้ตรงเวลาใหม่
- เมื่อพัฒนาส่วนนี้ ให้การแก้เวลา/ยกเลิกสัมพันธ์กับกำหนดการ และกำหนดวิธีจัดการ reminder ที่เลยเวลาส่งแล้ว
- worker ในอนาคตต้องตรวจสถานะ booking ก่อนส่ง มี retry/backoff และป้องกันส่งซ้ำ บันทึก `sent_at` ตามผลสำเร็จของผู้ให้บริการ
- provider, ช่องทาง, consent และนโยบายส่งซ้ำยังไม่กำหนด ห้ามเปิดส่งข้อความจริงโดยอาศัยเพียงข้อมูลตัวอย่าง

## 23. Agent Workflow

1. อ่าน `AGENTS.md`, README และโค้ดที่เกี่ยวข้อง ตรวจ `git status` ก่อนเริ่มเพื่อรักษางานที่มีอยู่
2. ตรวจว่าราก Git และ remote เป็น repo `Svinyoo/Lorjod` ตั้ง working directory ที่ราก repo ก่อนรันคำสั่ง
3. ตรวจพฤติกรรมจาก implementation และ tests แยกข้อเท็จจริงจากข้อเสนอ
4. แก้เฉพาะส่วนที่จำเป็น รักษาสัญญา API ข้อมูล และสิทธิ์ของทุกบทบาท
5. รันการตรวจที่เหมาะกับงาน ตรวจ diff และปรับเอกสารเมื่อพฤติกรรมเปลี่ยน
6. สรุปสิ่งที่เปลี่ยน ผลทดสอบ และข้อจำกัดที่ยังเหลือโดยไม่อ้างผลที่ไม่ได้ตรวจ

เมื่อแก้เอกสารนี้ อย่าถือว่าข้อเสนอที่ยังไม่ทำกลายเป็นความสามารถของระบบ และอย่าขยายงานเป็น refactor ทั้งโครงการโดยไม่มีเหตุผลตามคำขอ

## 24. Git & Branch Rules

- ตรวจ branch และการแก้ไขที่มีอยู่ก่อนทำงาน ห้ามลบหรือย้อนงานของผู้ใช้ที่ไม่เกี่ยวข้อง
- หากต้องสร้าง branch ใช้ `codex/<short-description>` เว้นแต่ผู้ใช้กำหนดชื่ออื่น
- commit ให้มีขอบเขตเดียวและข้อความอธิบายผลของงาน หลีกเลี่ยงรวมไฟล์ generated, database หรือ secrets
- ไม่ commit, push, merge หรือ deploy โดยอนุมานจากคำขอแก้ไฟล์เพียงอย่างเดียว ให้ทำตามขอบเขตที่ผู้ใช้มอบหมาย
- ห้าม force push, reset แบบทำลายข้อมูล หรือ rewrite ประวัติร่วมโดยไม่มีคำสั่งที่ครอบคลุมการกระทำนั้น
- PR อธิบายปัญหา พฤติกรรมหลังแก้ วิธีทดสอบ และ migration/ข้อจำกัดที่ผู้ตรวจต้องรู้

## 25. Definition of Done

- งานตรงกับคำขอและเกณฑ์ยอมรับ ไม่มีการเปลี่ยนกติกาธุรกิจนอกขอบเขต
- พฤติกรรมที่เปลี่ยนตรวจสอบได้ สิทธิ์และข้อมูลข้ามบทบาทไม่เสียหาย
- การทดสอบที่เกี่ยวข้องผ่าน หรือแจ้งเหตุที่ตรวจไม่ได้พร้อมความเสี่ยงอย่างชัดเจน
- หากแก้ UI ได้ตรวจสถานะ loading/empty/error และการใช้งานตาม flow ที่กระทบ
- หากแก้ schema มี migration ที่รักษาข้อมูลและตรวจการอ้างอิงแล้ว
- เอกสาร/API ที่เกี่ยวข้องตรงกับผลจริง ไม่มี secret หรือข้อมูลทดสอบปะปนในข้อมูลใช้งาน
- ตรวจ diff แล้วและแจ้งผลให้ผู้ใช้ การเสร็จงานพัฒนาไม่ได้แปลว่ามีการ deploy แล้ว

## 26. Important Restrictions

- ห้ามลบ/รีเซ็ตฐานข้อมูลจริง seed ทับข้อมูลเดิม หรือโอนเจ้าของลานโดยอัตโนมัติ
- ห้ามเปิดสมัคร Admin สาธารณะ ยกระดับสิทธิ์จาก client หรือข้าม owner check เพื่อให้ flow ผ่าน
- ห้ามตัด audit/เหตุผล/การตรวจข้อมูลเก่าออกจากการแก้ไขของ Admin
- ห้ามถือว่าราคาจาก browser จำนวนช่องบนหน้าค้นหา หรือ quote เป็นข้อยืนยันสุดท้ายตอนจอง
- ห้ามอ้างว่ารับเงินจริง ส่ง push จริง เปิดประตูจริง หรือป้องกัน concurrent booking สมบูรณ์ หากยังไม่มี implementation และหลักฐานทดสอบ
- ห้ามเปลี่ยน timezone ที่จัดเก็บ หน่วยเงิน ชื่อสถานะ หรือเส้นทาง API โดยไม่ประเมินข้อมูลและผู้เรียกเดิม
- ห้ามแก้ `node_modules/` เป็นวิธีถาวร หรือย้ายโค้ดจาก repository ของโฟลเดอร์แม่มารวมโดยไม่มีข้อกำหนด

## 27. When Requirements Are Unclear

- เริ่มจากอ่านโค้ด tests และ README เพื่อหาคำตอบก่อนถามซ้ำ
- หากเป็นรายละเอียดเล็กน้อยที่ย้อนกลับได้ ให้เลือกตามรูปแบบเดิม ระบุสมมติฐาน และเดินหน้าทำงาน
- หากกระทบราคา การเงิน สิทธิ์ การยกเลิก/คืนเงิน การสูญหายของข้อมูล หรือการเปลี่ยนสถานะ ให้ถามเฉพาะประเด็นที่ต้องตัดสินใจก่อนแก้ส่วนที่ขึ้นกับคำตอบ และทำส่วนอิสระต่อได้
- หากเอกสารกับโค้ดต่างกัน ให้ระบุพฤติกรรมปัจจุบันและทางเลือก ไม่แก้โค้ดให้ตรงข้อความโดยอัตโนมัติ
- อย่าสร้างนโยบายธุรกิจ สัญญากับผู้ให้บริการ หรือเป้าหมาย production แทนผู้ใช้ บันทึกสิ่งที่ยังไม่ตกลงให้ชัดเจน
